# This file can be left empty or can be used to expose parts of the package.
# For now, let's ensure Python treats 'src' as a package.

# You could also modify sys.path here if needed, but it's generally better
# to manage PYTHONPATH externally or use package installation.

print("coding_agent.src package initialized")
